# Fix ReferenceError in Berto JS

## Steps

- [x] 1. Diagnose issue: `initSetup()` and `initArtifactResizer()` are called but never defined
- [x] 2. Confirm plan with user
- [ ] 3. Add `initSetup()` function to `Berto/js/main.js` (first-time setup overlay logic)
- [ ] 4. Add `initArtifactResizer()` function to `Berto/js/main.js` (draggable drawer resize logic)
- [ ] 5. Verify no `ReferenceError` remains

